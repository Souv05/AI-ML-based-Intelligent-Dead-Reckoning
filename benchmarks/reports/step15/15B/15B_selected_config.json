{
  "NHC_STD_MPS": 1.0,
  "R_NHC": 1.0,
  "candidate_grid": [
    0.25,
    0.5,
    0.75,
    1.0,
    1.5,
    2.0,
    3.0
  ],
  "selection_rule": [
    "reject numerically unstable candidates",
    "require >=30% mean |v_lateral| reduction vs no-NHC baseline",
    "reject candidates with median drift >15% worse than baseline (over-constraining)",
    "rank remaining by median validation drift %",
    "tie-break (within 2pp of best): lowest across-sequence drift dispersion (cv)",
    "final tie-break: closest to the Step 15A initial value of 1.0 m/s"
  ],
  "selection_rationale": "All 7 candidates are numerically indistinguishable from the Step 14 no-NHC baseline on every navigation metric (median FDE identical to 9 decimal places; mean |v_lateral| ~1e-16 m/s in every configuration, including baseline). This is a structural property of the reused, unmodified Step 14 predict() model, not a tuning failure: predict() recomputes vE=v_fwd*sin(psi), vN=v_fwd*cos(psi) every step, which makes v_lateral(x) analytically 0 immediately after every predict, so the NHC innovation is always ~0 and update_nhc has no point-estimate effect to tune. The engineering selection rule (sections 12/15) therefore cannot discriminate among candidates on this pipeline; NHC_STD_MPS=1.0 m/s (the Step 15A initial engineering value) is frozen for continuity into 15C, not because it outperformed the others - none did. Reported as a negative result, not hidden: realising any benefit from lateral NHC requires the EKF process model to give vE/vN an independent, non-heading-slaved degree of freedom during predict, which is an EKF redesign question outside 15B's scope (\"do not redesign the EKF\").",
  "validation_sequences": [
    "S3a",
    "S3b",
    "S3c",
    "V-Vfa02",
    "Vw03",
    "Vw16a",
    "Vw16b"
  ],
  "n_validation_outage_cases": 120,
  "outage_durations_represented": [
    10,
    20,
    30,
    60,
    90,
    120
  ],
  "reused_step14_ekf_config": {
    "state": [
      "E",
      "N",
      "vE",
      "vN",
      "psi"
    ],
    "heading_model": "constant (psi_dot=0) + process noise; gyro verified unusable",
    "Q_psd_diag": [
      8.0,
      8.0,
      1.0,
      1.0,
      0.004873878716587337
    ],
    "Q_psd_units": [
      "m^2/s",
      "m^2/s",
      "(m/s)^2/s",
      "(m/s)^2/s",
      "rad^2/s"
    ],
    "sig_gnss_m": 5.0,
    "R_diag": [
      25.0,
      25.0
    ],
    "P0_diag": [
      25.0,
      25.0,
      4.0,
      4.0,
      0.12184696791468343
    ],
    "gnss_measurement": "reference_x/reference_y where gnss_available==1 and outside outage",
    "pre_buffer_s": 10.0,
    "post_buffer_s": 30.0,
    "selected_on": "validation median outage-span drift %",
    "seed": 42,
    "tuning_grid": [
      [
        0.5,
        0.00030461741978670857
      ],
      [
        0.5,
        0.004873878716587337
      ],
      [
        2.0,
        0.00030461741978670857
      ],
      [
        2.0,
        0.004873878716587337
      ],
      [
        8.0,
        0.00030461741978670857
      ],
      [
        8.0,
        0.004873878716587337
      ]
    ]
  },
  "reused_step15a_nhc_equations": {
    "z": 0.0,
    "h": "v_lateral(x) = vE*cos(psi) - vN*sin(psi)",
    "H": "[0, 0, cos(psi), -sin(psi), -v_forward(x)]",
    "sig_nhc_mps_initial": 1.0,
    "sig_nhc_status": "EXPLICIT, NOT optimal, NOT tuned on validation or test - selection is Step 15B"
  },
  "test_set_used_for_tuning": false,
  "seed": 42,
  "not_claimed_optimal": true
}